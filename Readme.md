### step 1:
    ```
    pip install -r requirements.txt
    ```
    
### step 2:
    ```
    python main.py
    ```
    Saves parsed outputs in outputs/{timestamp}_output folder as {pdf_name}.json file.
